import { InterpreterScreen } from "./features/interpreter/screens/interpreter-screen";

/** App Router 진입점은 기능 화면을 연결하는 역할만 담당합니다. */
export default function Home() {
  return <InterpreterScreen />;
}
